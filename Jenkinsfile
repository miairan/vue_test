pipeline {
    agent any

    triggers {
        githubPush()
    }

    stages {
        stage('Build') {
            steps {
                echo "✅ Webhook Triggered hhh - 构建成功！"
            }
        }
    }
}
